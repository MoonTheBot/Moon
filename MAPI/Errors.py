import json
import os

class MErrors():
    if cooldown(error, commandName):
        print("You are on cooldown for the " + commandName + " command.")
        break

    if apiDown(status, statusCode):
        print("The API is currently down and I received a status code of " + statusCode + ".")
        break

    # Need To Finish This Handler
    
    else:
        print("An unexpected error has occurred.")
